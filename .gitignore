.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.env
.env.*
!.env.example
.aws/
.aws/credentials
.aws/config
artifacts/local/
sessions/local/
*.sqlite3
*.db
.DS_Store

# Demo recordings and their intermediates are large and regenerable. The scripts that
# produce them are source and must stay tracked, so ignore the media, not the folder.
captures/*.webm
captures/*.mp4
captures/*.mov
captures/frames/
*.webm
*.mp4
*.mov
node_modules/
